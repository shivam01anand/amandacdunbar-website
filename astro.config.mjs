// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// No integrations needed for this simple site yet
	integrations: [],
	// Add other Astro config options here if needed later
	// Example: site: 'https://amandacdunbar.com/'
});
